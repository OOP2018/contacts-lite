# Properties used in the application

# Location of database. For H2 this should be a directory name.
database.url = jdbc:h2:/home/jim/temp/h2/contacts

# Create tables on start-up if they don't exist?
createtables = true