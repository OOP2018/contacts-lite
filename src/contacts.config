# Properties used in the application

# Location of database. For H2 this is the basename of
# files in the database.
jdbc.url = jdbc:h2:/home/jim/temp/h2/contacts
# For an embedded database you don't need a user or password.
# For client-server database like MySQL, you do need a user and password.
# Don't use root or admin user
jdbc.user = 
jdbc.password =

# Create tables on start-up if they don't exist?
createtables = true
